# Backend Configuration Template
# Copy this file to backend-config.hcl and add your S3 bucket name
# DO NOT commit backend-config.hcl to Git (it's in .gitignore)

bucket = "YOUR-BUCKET-NAME-HERE"

# Optional: Override other backend settings
# region         = "us-east-1"
# encrypt        = true
# dynamodb_table = "terraform-state-lock"
